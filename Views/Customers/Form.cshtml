@model Vidly.ViewModels.CustomerFormViewModel
@{
    ViewData["Title"] = "New";
}

<h2>New Customer</h2>

<div class="container">
    <div class="col-6">
        @using (Html.BeginForm("Save", "Customers"))
        {
            <div class="form-group">
                @Html.LabelFor(m => m.Customer.Name, new{@class = "form-label mt-2"})
                @Html.TextBoxFor(m => m.Customer.Name, new {@class = "form-control"})
            </div>
            <div class="form-group">
                @Html.LabelFor(m => m.Customer.BirthDate, new{@class = "form-label mt-2"})
                @Html.TextBoxFor(m => m.Customer.BirthDate, "{0:d MMM yyyy}", new {@class = "form-control"})
            </div>    
            <div class="form-group">
                @Html.LabelFor(m => m.Customer.MembershipTypeId, new{@class = "form-label mt-2"})
                @Html.DropDownListFor(m => m.Customer.MembershipTypeId, new SelectList(Model.MembershipTypes, "Id", "Name"), "", new { @class = "form-control" })
            </div>   
            <div class="checkbox mt-2">
                @Html.CheckBoxFor(m => m.Customer.IsSubscribedToNewsletter) Subscribed to Newsletter?
            </div>
            @Html.HiddenFor(m => m.Customer.Id)
            <button type="submit" class="btn btn-primary mt-4">Save</button>
        }
    </div>
</div>
